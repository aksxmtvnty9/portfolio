---
import { SITE } from "@consts";
---

<footer
  class="relative max-h-2xl lg:min-h-[550px] md:min-h-[420px] min-h-[350px] flex flex-col justify-end-safe gap-8"
>
  <!-- Background: Full-width mountain SVG -->
  <img
    src="/images/mountain-bg.svg"
    alt="Mountains"
    class="absolute inset-0 w-full h-full object-cover object-top"
  />

  <!-- Content overlay -->
  <div class="relative z-10 mx-auto max-w-2xl w-full px-4">
    <!-- Row 1: Let's Connect + Performance Stats -->
    <div class="flex justify-between items-start mb-8">
      <div>
        <h3 class="text-lg font-semibold mb-1 text-[#2A1E14]">Let's Connect</h3>
        <p class="text-sm text-[#3E2723]">Interested in working together?</p>
      </div>
    </div>
  </div>

  <div class="relative z-10 mx-auto max-w-2xl w-full px-4 mb-4">
    <div class="flex justify-between items-center">
      <p class="text-[#4A3728] text-sm">
        © {new Date().getFullYear()}
        {SITE.NAME}
      </p>

      <span id="load-time" class="text-sm text-[#3E2723] hidden md:inline lg:inline"
        >Loaded in --</span
      >

      <!-- Theme Toggles -->
      <div class="flex items-center gap-1">
        <button
          id="theme-light"
          class="p-2 rounded-md text-[#3E2723] hover:text-[#2A1E14] hover:bg-[#2A1E14]/10 transition-colors"
          aria-label="Light theme"
          title="Light"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <circle cx="12" cy="12" r="5"></circle>
            <line x1="12" y1="1" x2="12" y2="3"></line>
            <line x1="12" y1="21" x2="12" y2="23"></line>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
            <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
            <line x1="1" y1="12" x2="3" y2="12"></line>
            <line x1="21" y1="12" x2="23" y2="12"></line>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
            <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
          </svg>
        </button>
        <button
          id="theme-dark"
          class="p-2 rounded-md text-[#3E2723] hover:text-[#2A1E14] hover:bg-[#2A1E14]/10 transition-colors"
          aria-label="Dark theme"
          title="Dark"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
          </svg>
        </button>
        <button
          id="theme-system"
          class="p-2 rounded-md text-[#3E2723] hover:text-[#2A1E14] hover:bg-[#2A1E14]/10 transition-colors"
          aria-label="System theme"
          title="System"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
            <line x1="8" y1="21" x2="16" y2="21"></line>
            <line x1="12" y1="17" x2="12" y2="21"></line>
          </svg>
        </button>
      </div>
    </div>

    <div id="load-time" class="text-sm w-full text-center md:hidden lg:hidden text-[#3E2723]">
      Loaded in --
    </div>
  </div>
</footer>

<script is:inline>
  // Performance timing - show load time
  (function () {
    function updateLoadTime() {
      const loadTimeEl = document.getElementById("load-time");
      if (!loadTimeEl) return;

      if (performance.timing && performance.timing.loadEventEnd > 0) {
        const loadTime =
          (performance.timing.loadEventEnd -
            performance.timing.navigationStart) /
          1000;
        loadTimeEl.textContent = "Loaded in " + loadTime.toFixed(2) + "s";
      } else {
        const perfData = performance.getEntriesByType("navigation")[0];
        if (perfData) {
          const loadTime = perfData.loadEventEnd / 1000;
          loadTimeEl.textContent = "Loaded in " + loadTime.toFixed(2) + "s";
        } else {
          loadTimeEl.textContent = "Loaded in 0.00s";
        }
      }
    }

    if (document.readyState === "complete") {
      updateLoadTime();
    } else {
      window.addEventListener("load", updateLoadTime);
    }
  })();

  // Theme switching
  function setTheme(theme) {
    localStorage.theme = theme;

    const dark =
      theme === "dark" ||
      (theme === "system" &&
        window.matchMedia("(prefers-color-scheme: dark)").matches);

    if (dark) {
      document.documentElement.classList.add("dark");
    } else {
      document.documentElement.classList.remove("dark");
    }

    updateActiveButton(theme);
  }

  function updateActiveButton(theme) {
    const buttons = {
      light: document.getElementById("theme-light"),
      dark: document.getElementById("theme-dark"),
      system: document.getElementById("theme-system"),
    };

    Object.keys(buttons).forEach(function (key) {
      const button = buttons[key];
      if (button) {
        if (key === theme) {
          button.classList.add("text-[#2A1E14]", "bg-[#2A1E14]/20");
          button.classList.remove("text-[#3E2723]");
        } else {
          button.classList.remove("text-[#2A1E14]", "bg-[#2A1E14]/20");
          button.classList.add("text-[#3E2723]");
        }
      }
    });
  }

  const currentTheme = localStorage.theme || "system";
  updateActiveButton(currentTheme);

  document
    .getElementById("theme-light")
    ?.addEventListener("click", function () {
      setTheme("light");
    });
  document.getElementById("theme-dark")?.addEventListener("click", function () {
    setTheme("dark");
  });
  document
    .getElementById("theme-system")
    ?.addEventListener("click", function () {
      setTheme("system");
    });
</script>
